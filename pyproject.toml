[project]
name = "model-comparison-extraction"
version = "0.1.0"
description = "Run a Baseten model comparison for structured job posting extraction."
requires-python = ">=3.11"

[tool.pyright]
venvPath = "."
venv = ".venv"
